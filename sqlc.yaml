version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/models/queries"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "internal/repository/db"
        overrides:
          - column: "trainers.average_rating"
            go_type: "database/sql.NullFloat64"