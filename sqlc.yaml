version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/models/queries"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "internal/repository/db"
        overrides:
          - db_type: "uuid"
            go_type: "string"
