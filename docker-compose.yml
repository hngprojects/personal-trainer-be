services:
  redis:
    image: redis:7-alpine
    container_name: personal_trainer_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data: